<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.financeapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FinanceApplication">
        <activity android:name=".sektorel"></activity>
        <activity android:name=".analyse" />
        <activity android:name=".defterdegeri" />
        <activity android:name=".babil" />
        <activity android:name=".kitap2" />
        <activity android:name=".fincankulp" />
        <activity android:name=".yatirimkitaplari" />
        <activity android:name=".Shortterm" />
        <activity android:name=".mav" />
        <activity android:name=".Longterm" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>